<!-- /autoplan restore point: ~/.gstack/projects/PMartinGatica-hidro-self-service/main-autoplan-restore-20260905-214746.md -->
# Design: Reconciliación de pagos (Fase 1)

Generated by /office-hours on 2026-09-05
Branch: main
Repo: PMartinGatica/coperativaremis-hidrolavadora
Status: APPROVED
Mode: Startup (intrapreneurship)

## Problem Statement

Verificado ejecutando la máquina de estados compilada: una sesión en `PAYMENT_PENDING` es forzada
a `PAYMENT_EXPIRED` por el barrido (`sweepExpired`, `sessionService.ts:333`) a los 120 segundos,
contados desde que el cliente carga la patente — no desde que abre el checkout de Mercado Pago.
`PAYMENT_EXPIRED` es un estado terminal sin transiciones de salida (`TRANSITIONS.PAYMENT_EXPIRED: []`,
`packages/state-machine/src/index.ts:28`). Si el webhook de MP se pierde o llega tarde — algo
realista en un pago lento normal (escanear QR, abrir la app, loguearse, pagar, con señal de una
parada de remises), fácil de superar en 2 minutos — el sistema escribe para siempre que el cliente
no pagó, sin forma de revertirlo.

Landmine relacionada: `getPayment()` en la interfaz `PaymentProvider` consulta una *preference*
(no un pago) y devuelve `status: 'PENDING'` hardcodeado. Hoy no lo llama nadie; sería peligroso si
alguien lo cablea como fallback creyendo que funciona.

## Demand Evidence

No aplica en el sentido de mercado — esto es una herramienta interna ya aprobada por el dueño de la
cooperativa (tarifas fijadas 2026-09-01), no una validación de si el producto debe existir. La
evidencia real es la ejecución del código: el estado terminal y el punto de referencia del timeout
se verificaron corriendo la máquina de estados compilada, no leyendo el código.

## Status Quo

Pregunta forzada (Q2): ¿qué le pasa hoy, en la práctica, a un cliente frente a la máquina si se
pierde el webhook? Respuesta textual del usuario: *"La hidrolavadora se encuentra dentro del taller
de la agencia donde atienden 24 horas y siempre hay gente (...) tendría que poder chequear por el
dashboard (...) mesa de entrada (...) a través de la base de datos tendría que saber si pagó o no
pagó. En el caso que pagó y no funciona tiene que tener una opción desde la mesa de entrada para
poder habilitarlo."*

Verificado en el código (`apps/api/src/services/adminService.ts`): mesa de entrada hoy puede LISTAR
y VER sesiones y pagos, pero no existe ninguna acción de aprobar/habilitar una sesión trabada. Hay
`emergencyStop`, que corta, no que habilita. El status quo real no es "sin backstop humano" — hay
un empleado 24hs con acceso a la base — es "backstop sin herramienta".

## Target User & Narrowest Wedge

El usuario forzado por Q4 no es un cliente final sino el operador interno: mesa de entrada del
taller, que necesita el camino más angosto para no quedarse sin qué hacer frente a un cliente el
día 1 de producción. Wedge elegido: una acción de aprobación manual de sesión, con lookup real
contra Mercado Pago (nunca un checkbox ciego), que entra al núcleo de la Fase 1 en vez de quedar
para una fase aparte — decidido en D6 de esta sesión.

## Flujo de aprobación manual (mecanismo exacto)

Dos pasos, no uno (agregado en `/autoplan` tras el hallazgo del subagente CEO — Approach C no
se descartó del todo, se usa como primer paso de bajo riesgo antes de B):

1. **Reintentar automáticamente** (nuevo, bajo fricción): mesa de entrada aprieta un botón que
   dispara la MISMA `searchByExternalReference` que ya usa el barrido — sin ID, sin tipeo. Si MP
   confirma un pago aprobado, se resuelve solo. Sigue siendo una consulta real y positiva contra MP,
   no un checkbox — el riesgo de abuso no se reabre.
2. **Aprobación manual con ID real** (si el paso 1 no encuentra nada): mesa de entrada escribe el ID
   real de pago de Mercado Pago (visible en su propio dashboard/notificación de MP) en la ruta admin
   nueva. El backend valida ese ID contra `getPaymentById` (mismo método que ya usa
   `webhookRoutes.ts`), confirma `external_reference === sessionId` y el importe, y solo entonces llama
   a `processApproval()` — el mismo camino único de aprobación que usa el webhook. Esto es lo que cierra
   el vector de abuso que señaló Codex: no hay forma de autorizar sin una consulta real y positiva a MP.

Identidad y auditoría de quién aprueba: **ya existe**, no es trabajo nuevo. `admin_users`
(`db/schema.ts:202-206`) tiene email/password/role por persona, y `login()`
(`adminService.ts:66-74`) ya audita `actor: email` en cada acción. La nueva ruta de aprobación
manual reusa ese mismo patrón (`insertAudit` con el email del admin logueado, el sessionId, el ID de
pago de MP y el resultado) — no requiere construir un sistema de identidad desde cero.

## Constraints

- No se toca `firmware/` ni hardware — no aplica la puerta (a) reforzada de compilación de este
  Mundo (ADR-006).
- Reembolsos (`refundPayment()`) siguen siendo stub — política pendiente del dueño.
- La identidad de la patente (ADR-007) es otra fase, no ésta.
- Carpetas: `apps/api/src/payments/`, `apps/api/src/services/`, `packages/state-machine/`,
  `packages/shared/src/constants.ts`, `apps/api/tests/`, más la nueva ruta admin de aprobación
  manual.

## Premises

1. El defecto es real y verificado ejecutando, no hipotético.
2. Es resoluble ahora, sin esperar al dueño: `external_reference` ya existe en la preference
   (`createPayment`), y el webhook en vivo ya re-consulta correctamente contra MP antes de confiar
   en el body (`webhookRoutes.ts:47-51`) — el hueco es solo cuando el webhook nunca llega.
3. La aprobación manual de mesa de entrada es núcleo de la Fase 1, no un extra.
4. No aplica la puerta (a) reforzada de firmware.
5. Reembolsos quedan fuera: esta fase deja de mentir sobre si se pagó; no decide qué hacer con la
   plata si el lavado falla después de autorizado.

**Alcance recortado en esta sesión (D10):** `TRUST_PROXY`/`CF-Connecting-IP` (ADR-022), el conteo
del cupo diario contra fallas del sistema (ADR-024) y las guardas de arranque en producción
(ADR-025/026) — presentes en el borrador original de `fases/FASE-1.md` — se sacan a una Fase 1.5.
Son dominios de falla independientes de la reconciliación de pagos; mezclarlos agranda el diff que
pasa por `/review` + `/cso` sin compartir código con el fix real.

## Cross-Model Perspective

Segunda opinión de Codex (independiente, sin ver esta conversación): recomendó un comando canónico
`confirmPayment()` invocado tanto por el webhook tardío como por la aprobación manual, para que no
se conviertan en dos caminos divergentes con auditoría inconsistente. Al verificarlo en el código,
esa función canónica **ya existe**: `processApproval()` (`paymentService.ts:52`), documentada en el
propio código como "PUNTO ÚNICO de aprobación de pagos", idempotente por construcción (constraints
UNIQUE + lock `FOR UPDATE`). Ya maneja el caso de pago tardío sobre sesión terminal
(`paymentService.ts:271-279`) — pero solo para *registrar* el pago como aprobado, sin recuperar la
sesión. El trabajo real es extender esa rama, no inventar un comando nuevo.

Codex también señaló riesgo de abuso en la aprobación manual (botón de lavado gratis para un
empleado deshonesto o coaccionado) — incorporado como requisito: lookup real del ID de pago de MP,
nunca un checkbox ciego, con auditoría inmutable.

Un punto de Codex fue corregido, no adoptado tal cual: propuso mover el origen del timeout a "cuando
se crea la preference" en vez de "cuando se carga la patente". Verificado en
`sessionService.ts:56-135`: `createSessionWithPayment` crea la sesión y llama a
`provider.createPayment` en la misma función — son el mismo instante. Mover el punto de referencia
no cambia nada acá. Lo que sí es válido de su punto 3: la ventana debe ampliarse sustancialmente
(sugerencia: 10-15 min en vez de 120s), independientemente del punto de referencia.

## Approaches Considered

**Nota de precisión sobre `authFlow`:** el bloque de recuperación que ya existe
(`paymentService.ts:240-269`, array `authFlow = ['PAYMENT_APPROVED', 'AUTHORIZED',
'WAITING_FOR_BUTTON', 'RUNNING']`) resuelve un caso distinto: un webhook duplicado/retrasado sobre
una sesión que **ya avanzó** más allá de `PAYMENT_PENDING`. El caso de esta fase es otro: una sesión
que cayó en la rama terminal (`paymentService.ts:271-279`), que hoy solo *registra* el pago sin
recuperar la sesión. Los approaches A y B agregan una rama nueva junto a esa, no modifican `authFlow`.

### Approach A: Mínimo viable — parche inline
Extender `processApproval()` con un `if session.status === 'PAYMENT_EXPIRED'` en la rama terminal
(`paymentService.ts:271-279`), en paralelo al bloque de `authFlow`. Reutiliza el 100% de la
idempotencia y auditoría probadas. Effort: human ~1 día / CC ~45 min. Riesgo: la regla de qué
estados vencidos son recuperables queda como un if más en una función de 280 líneas que mueve
dinero real.

### Approach B: Arquitectura ideal — tabla exhaustiva por estado (ELEGIDA)
Igual que A, pero la regla "qué estado terminal puede recuperar un pago tardío" vive en una
función/tabla propia, con un test por cada `SessionStatus` terminal existente. Superficie de cambio
similar a A (mismos 2 llamadores nuevos: sweep + ruta admin), con ~50% más esfuerzo por escribir la
regla de forma exhaustiva en vez de un solo if. Effort: human ~1.5 días / CC ~1h.

**Tabla exhaustiva** (los 8 `SessionStatus` con `TRANSITIONS[...] === []`, `state-machine/src/index.ts:26-33`):

| Estado terminal | ¿Recupera pago tardío? | Por qué |
|---|---|---|
| `PAYMENT_EXPIRED` | **Sí** | Es el bug de esta fase: el webhook se perdió sobre un pago que sí se acreditó. |
| `PAYMENT_FAILED` | No | MP ya rechazó explícitamente ese pago (o hubo `amount_mismatch`); una aprobación tardía sobre el mismo ID sería anómala — señal para revisión humana, no autorización automática. |
| `MACHINE_OFFLINE` | No | Regla de negocio ya deliberada (`state-machine/src/index.ts:21` y `paymentService.ts:179-210`): si la máquina cayó entre el pago y la aprobación, la autorización se revoca a propósito. Reabrirla es política de reembolso (premisa 5, fuera de alcance). |
| `AUTHORIZATION_EXPIRED` | No | El pago ya fue aprobado con éxito antes de llegar acá; lo que expiró es la ventana para apretar el pulsador, no el pago. Problema distinto, no es reconciliación de pagos. |
| `SESSION_INTERRUPTED` | No | El lavado ya arrancó y se interrumpió; es el caso de reembolso de ADR-024, movido a Fase 1.5. |
| `EMERGENCY_STOP` | No | Acción deliberada de un admin. No es un problema de pago. |
| `DEVICE_ERROR` | No | Falla reportada por el ESP32, no del proveedor de pago. |
| `FINISHED` | N/A | El pago ya se resolvió con éxito antes; no hay nada que reconciliar. |

De los 8 estados terminales, solo uno es recuperable por esta vía — la exhaustividad de B no agrega
trabajo por los otros 7, documenta por qué correctamente NO se tocan.

### Approach C: Lateral — sin botón de autorización nueva
Mesa de entrada solo obtiene un botón "volver a chequear ahora" que dispara la misma búsqueda
automática del sweep, sin ninguna acción privilegiada de aprobación. Elimina el riesgo de abuso por
completo, pero renuncia a lo que el usuario pidió explícitamente en Q2/D6 (una opción real para
habilitar la sesión). Rechazada por eso — no por ser mala idea técnica, sino por no responder al
requisito ya establecido en esta misma sesión.

## Recommended Approach

**B.** Superficie similar a A (~50% más esfuerzo, no "mismo costo"), mismos dos llamadores nuevos (sweep con búsqueda por
`external_reference`, ruta admin de aprobación manual con lookup real de pago), pero con la regla de
recuperación por estado escrita como tabla exhaustiva con test por `SessionStatus` — para que el
próximo estado que se agregue a la máquina de estados fuerce una decisión explícita en vez de colar
silenciosamente un comportamiento no revisado, en la función que autoriza el uso de la máquina.

## Política de reintento del sweep (no es reintento indefinido)

El barrido busca por `external_reference` **una sola vez por sesión**, en el momento en que esa
sesión cruza la ventana de expiración — no en cada ciclo de sweep indefinidamente. Esto acota el
volumen de llamadas al endpoint de búsqueda de MP a "sesiones que cruzan el timeout", que escala con
el volumen de lavados, no con la frecuencia de polling del sweep (relevante multi-máquina desde el
día uno). Si el pago se acredita en MP *después* de esa única consulta, la recuperación queda a
cargo de la vía manual (Approach B), que no depende de una ventana de reintento — es exactamente
para eso que existe.

## Open Questions

- **RESUELTO en `/autoplan`:** el timeout de 120s pasa a **600s (10 min)** como default de esta
  fase, no como pregunta abierta — es un parámetro revisable, no una política de negocio inventada.
  El dueño puede ajustarlo después; no bloquea el diseño ni el Build.
- **RESUELTO en `/autoplan`:** mesa de entrada usa cuentas **individuales** de `admin_users`, no una
  compartida — pasa a Success Criteria (ver abajo), porque sin eso la auditoría de "quién aprobó" es
  decorativa (hallazgo del CEO review). Aprovisionar esas cuentas es tarea humana, no de código.
- El código de demo/test para `PaymentProvider` necesita su propio `searchByExternalReference` —
  confirmar que el `demoProvider.ts` puede simular tanto el caso "encontrado" como "no encontrado".

## Success Criteria

- Un pago aprobado en Mercado Pago, con webhook nunca entregado, termina autorizado (no en
  `PAYMENT_EXPIRED`) tras el barrido con búsqueda.
- Un pago aprobado después del timeout original es recuperable vía "reintentar automáticamente" o,
  si eso no alcanza, vía aprobación manual de mesa de entrada, con auditoría de quién aprobó y contra
  qué ID de pago real — usando una cuenta individual de `admin_users`, no la compartida por defecto.
- Ningún camino nuevo permite autorizar sin una consulta real y positiva a Mercado Pago.
- Los índices únicos del ADR-003 (una sesión activa por máquina, etc.) siguen intactos — incluyendo
  el caso en que otra sesión ya está activa en la misma máquina cuando se intenta recuperar una vieja
  (ver Sección de Ingeniería más abajo).
- `getPayment()` deja de devolver `PENDING` hardcodeado: se corrige para usar `getPaymentById` +
  búsqueda real, o se elimina si nada lo necesita. No puede cerrarse la fase con la trampa viva.
- El timeout de expiración pasa de 120s a 600s (10 min), contado desde el mismo `createdAt` de
  siempre (no hay un evento distinto de "checkout abierto" que capturar — verificado, son el mismo
  instante).
- Toda sesión recuperada (automática o manualmente) queda etiquetada de forma distinguible en el
  audit log (`PAYMENT_MANUALLY_RECONCILED` u otra acción propia) — no solo para auditoría, sirve como
  métrica consultable de "pagó pero casi no se lava", sin construir infraestructura de notificación
  nueva.
- Suite completa verde (`npm test`); esta fase no toca `firmware/`.

## Dependencies

Ninguna decisión ajena bloquea el Build de esta fase (premisa 2). La Fase 1.5 (TRUST_PROXY, cupo
diario, guardas de arranque) queda documentada como trabajo de seguimiento, ya medido, lista para
retomar sin re-diagnosticar.

**No-código, antes de producción:** aprovisionar cuentas individuales de `admin_users` para mesa de
entrada (arriba). **Conversación con el dueño al momento de desplegar** (hallazgo del CEO review,
riesgo de adopción): confirmar que la llave física sigue siendo *solo* para emergencia — si después
del lanzamiento se usa seguido, es señal de que la herramienta de reconciliación no alcanza, no un
hecho aislado a ignorar.

## The Assignment

Antes de tocar código: correr `/autoplan` sobre este design doc (CEO → design → eng) para bajarlo a
tareas concretas de implementación — es el siguiente paso obligatorio del pipeline gstack de este
Mundo (`office-hours → autoplan → build → review+cso → qa → ship → retro`), no un paso opcional.

## What I noticed about how you think

- Cuando te pregunté qué le pasa hoy a un cliente frente a la máquina, no contestaste en abstracto
  — describiste el taller real, el turno de 24hs, y lo que mesa de entrada necesitaría poder hacer
  con la base de datos. Esa es la clase de respuesta que hace que un diagnóstico sirva para algo.
- Elegiste la opción más completa (B) en vez de la más rápida (A) en una función que mueve dinero de
  la cooperativa, sabiendo que costaba más tiempo de CC — priorizaste que el próximo estado nuevo
  fuerce una decisión explícita, no que se cuele en silencio.
- Aceptaste sacar tres hallazgos ya escritos en el alcance original de la fase (TRUST_PROXY, cupo
  diario, guardas de arranque) para no mezclarlos con el fix real — no te aferraste al documento
  tal como estaba escrito solo porque ya estaba escrito.

---

## AUTOPLAN REVIEW

Run by `/autoplan` on 2026-09-05. Auto-decisions per the 6 decision principles; taste
decisions and user challenges (none found) surface at the Final Approval Gate below.
UI scope: no (only "dashboard" x2, both referring to Mercado Pago's own external
dashboard, not this product's UI — false positive, excluded). DX scope: no (raw hits on
"api/webhook/terminal/npm" are domain vocabulary — "terminal" means FSM terminal state,
not a CLI — not "this product is a developer tool"). Phases 2 and 2.5 skipped.

### Pre-Review System Audit

- `git log -30`: last 2 commits are this session's office-hours output (design doc
  APPROVED + a status-sync fix); before that, memory/infra commits (ADR, ESTADO,
  deploy guide). No prior code churn on the files this fase touches.
- `git diff main --stat`: empty except the restore-point comment just added — no
  feature branch cut yet, working directly on `main` pre-Build.
- `git stash list`: empty.
- `TODO/FIXME/HACK/XXX` grep across `apps/`, `packages/`: 2 hits, both false positives
  (Spanish "TODOS los endpoints" = "all", and one comment literally describing that a
  function body runs in one transaction). No real deferred-work debt in the codebase.
- Files touched in the last 30 days: almost entirely memory files (ESTADO, ADR, MAPA)
  plus `firmware/esp32/src/{app_config.h,api_client.h}` twice each. **No application
  code under `apps/api/src/` has churned recently** — the payment/session services are
  stable, not an area with a history of review-driven rework. No retrospective flag.
- `TODOS.md`: does not exist in this repo.
- Design doc: `docs/designs/reconciliacion-pagos.md` (this file), APPROVED, reviewed 2
  rounds by adversarial agent (10/10). Used as source of truth below — 0C-bis
  (Implementation Alternatives) and the premise challenge were **already run** in
  `/office-hours` (Approaches A/B/C, Approach B selected, premises 1-5 agreed). Per the
  DRY principle, not re-litigated from scratch here — referenced and built on.

**Taste calibration:**
- Well-designed: `processApproval()` (`paymentService.ts:52-281`) — single point of
  approval, idempotent by DB constraint (not by an `if`), named result types per branch,
  audit log at every branch. `webhookRoutes.ts` — never trusts the webhook body; HMAC
  verify then re-query the real payment before any business logic, documented in its own
  header comment. `state-machine/src/index.ts` `TRANSITIONS` table — one declarative
  source of truth for legal transitions, `canTransition()` as the sole gate.
- Anti-pattern: `getPayment()` on `PaymentProvider` — a stub masquerading as a real
  method (`status: 'PENDING'` hardcoded), uncalled today, a landmine for the next person
  who wires it in trusting the interface. Already in this doc's Success Criteria.

### 0A. Premise Challenge — already run in `/office-hours`

Re-verified, not re-run: (1) the defect is real, execution-verified — confirmed again
by reading `paymentService.ts:271-279` directly, which still writes `payment.status =
'APPROVED'` on a terminal session without recovering it, exactly as the design doc
says. (2) Resolvable without the owner — confirmed, no code path here depends on
ADR-007 or the refund policy. Nothing to add.

### 0B. Existing Code Leverage

| Sub-problem | Existing code that already solves it |
|---|---|
| Idempotent, audited payment approval | `processApproval()` — reused, not rebuilt (per Cross-Model Perspective above) |
| Re-verify a payment against MP before trusting it | `getPaymentById` + HMAC pattern in `webhookRoutes.ts` — same call reused for manual approval |
| Per-user identity + audit for admin actions | `admin_users` table + `insertAudit(actor: email)` — reused, not rebuilt (already in this doc) |
| Search a payment when you don't have its ID yet | `external_reference` already set at `createPayment` time — just needs a provider method to search by it |

Nothing here is being rebuilt from scratch; every piece is an extension of an existing,
working mechanism. This is the strongest form of "existing code leverage" — there was no
temptation to reinvent, because DRY was already checked in `/office-hours`.

### 0C. Dream State Mapping

```
CURRENT STATE                          THIS FASE                           12-MONTH IDEAL
Webhook lost -> false            -->   Webhook lost -> provider      -->   Every money-moving
"unpaid" verdict, terminal,            re-checked before writing            state transition in
irreversible. Front desk sees          "unpaid"; PAYMENT_EXPIRED           this system is
the payment in MP but has no           becomes recoverable via a           reconcilable against
tool to act on it.                     real, audited MP-lookup path.       its provider by
                                        Front desk has exactly one          construction — no
                                        button, gated by a real             other silent-terminal-
                                        payment ID, never a checkbox.       state bug can exist,
                                                                            because the pattern
                                                                            (verify then
                                                                            transition, never
                                                                            assume) is now the
                                                                            house style, not a
                                                                            one-off fix.
```

This fase moves toward the ideal: it doesn't just patch one state, it establishes the
pattern (provider re-verification before any terminal-state write) that the rest of the
system's money-handling code should follow.

### 0C-bis. Implementation Alternatives — already satisfied

Approach A/B/C were generated and reviewed in `/office-hours` (see Approaches Considered
above); the user selected B (exhaustive per-state table) after a 2-round adversarial
review of the design doc. Re-running this step would duplicate completed work — DRY
principle applies. No new alternatives needed; Section review below builds directly on
Approach B.

### 0D/0F. Mode Selection — SELECTIVE EXPANSION (autoplan override, mechanical)

`/autoplan`'s own override fixes mode to SELECTIVE EXPANSION for every run (not the
underlying skill's context-dependent default) — hold the office-hours-approved scope as
baseline, make it bulletproof, then scan for expansion candidates and auto-decide each
one individually via the 6 principles (P2/P3/P4) rather than silently adding or
dropping anything. Baseline (HOLD SCOPE analysis) below; expansion scan follows.

**Complexity check:** file count for Approach B: `payments/provider.ts` (interface),
`payments/mercadoPagoProvider.ts`, `payments/demoProvider.ts` (3), `services/
paymentService.ts`, `services/sessionService.ts`, `services/adminService.ts` (3),
`state-machine/src/index.ts` (1), `shared/src/constants.ts` (1), a new
`http/routes/adminRoutes.ts` endpoint (1), plus a new small module for the exhaustive
recoverability table (1) — **10 production files, 1 new file** (a lookup table + one
pure function, not a new class/service). This trips the literal ">8 files" smell
threshold, but for a structural reason, not scope creep: the shape of the fix is fixed
by an existing 2-implementation provider interface (mercadopago + demo) that both need
the same new method, and a payment/session/admin split that already existed before this
fase. There is no way to touch fewer files while still fixing both the automatic
(sweep) and manual (front-desk) paths against both providers. **Auto-decided: proceed
as-is, logged as a Taste decision** (surfaced at the Final Gate below) rather than
forcing an artificial reduction that would leave one provider or one path unfixed.

**Minimum set of changes:** exactly Approach B's scope from the design doc, plus the
race-condition closure found in this review (see Section review below) — that one is
not "expansion," it is completeness of the same feature (Eng Prime Directive #3: shadow
paths), so it is not run through the cherry-pick ceremony below.

**Expansion scan (SELECTIVE EXPANSION) — candidates, each auto-decided, not silently
added or dropped:**

| # | Candidate | Blast radius? | Effort | Decision | Why |
|---|---|---|---|---|---|
| 1 | Distinct audit action `PAYMENT_MANUALLY_RECONCILED` (vs `PAYMENT_APPROVED`) so manual interventions are visible at a glance in the audit log | Yes — same new route/function | CC ~10 min | **ADD** (P2) | In blast radius, trivially cheap, directly serves this fase's own auditability goal |
| 2 | Specific error messages on manual-approve mismatch (wrong session, amount mismatch) instead of a generic failure | Yes — same new route | CC ~20 min | **ADD** (P2) | In blast radius, cheap, front-desk is a real person who needs to know *why* it failed, not just that it did |
| 3 | Notify owner/front-desk automatically when the sweep auto-recovers a payment (so nobody has to notice by accident) | No — no notification channel exists yet | New infra | **DEFER → Fase 1.5/TODOS.md** (P3) | Outside blast radius; would require building a notification mechanism this fase doesn't otherwise need |
| 4 | Admin dashboard counter: "N sesiones recuperadas manualmente esta semana" | Borderline — touches the admin UI but is a new metric, not required by Success Criteria | New UI element | **DEFER → TODOS.md** (P3) | Nice-to-have, not blocking; the admin route itself has no UI-scope requirement per Phase 0 |
| 5 | Rate-limit/cooldown on manual approvals per admin per hour, as anti-abuse beyond the mandatory MP-ID lookup | No — needs new counting infra | New infra | **DEFER → TODOS.md** (P3) | The MP-ID lookup requirement (already in scope) is the load-bearing control; this would be defense-in-depth, not required to close the abuse vector Codex flagged |
| 6 | Generalize "verify against provider before any terminal write" into a system-wide pattern beyond payments | No — large refactor, many unrelated call sites | Multi-day | **DEFER → TODOS.md** (P3) | 10x-ambitious but far outside this fase's blast radius; correct as future architectural direction, not this fase |

Items 1-2 added to scope (mechanical, P2). Items 3-6 deferred to TODOS.md (created below,
since none existed) — written down per CEO Prime Directive #7 ("everything deferred
must be written down"), not left as vague intentions.

### 0E. Temporal Interrogation

```
HOUR 1 (foundations):    Add searchByExternalReference to PaymentProvider + both impls;
                          add the exhaustive recoverability table as its own tested
                          module (not inline in processApproval).
HOUR 2-3 (core logic):   Extend processApproval's terminal branch for PAYMENT_EXPIRED;
                          wire the new admin route through it; handle the
                          uq_sessions_active_machine race (see Section review) —
                          this is the one place implementers could silently skip a
                          shadow path if it isn't called out now.
HOUR 4-5 (integration):  Sweep changes (search-before-expire, once per session);
                          constants.ts timeout value change; wire audit action names
                          (expansion items 1-2).
HOUR 6+ (polish/tests):  Fix/remove getPayment(); full test matrix (see Test Plan
                          below) including the machine-race case and both providers'
                          search behavior (found/not-found).
```
(Human-team scale for orientation; CC + gstack compresses this to roughly 1-2 hours
total, per the design doc's own effort estimates — human ~1.5 days / CC ~1h for
Approach B, plus the two small expansion items above.)

### NOT in scope (reaffirmed + reasoned)

- Reembolsos (`refundPayment()` stub) — premise 5, owner's policy pending.
- Identidad de patente (ADR-007) — separate fase, owner-dependent.
- `firmware/` / hardware — premise 4, this fase's diff never enters that gate.
- TRUST_PROXY/ADR-022, cupo diario/ADR-024, guardas de arranque/ADR-025-026 — D10,
  Fase 1.5, independent failure domains.
- A UI/design pass on the admin panel — the new field is one input + one button on an
  existing admin screen; no new screen, no visual design system work needed (confirmed
  by the UI-scope grep finding no real matches).

### What already exists (do not rebuild)

`processApproval()`, `getPaymentById`, HMAC webhook validation, `admin_users` +
`insertAudit`, `external_reference` on the payment preference, the `TRANSITIONS` table
and `canTransition()` gate. All reused as-is; see 0B table above.

### CEO Dual Voices

**CLAUDE SUBAGENT (CEO — strategic independence):**

Reviewed cold (fresh context, plan file only, no prior conversation). Full findings:

1. **Right problem / 10x reframe (High):** the plan defers the highest-leverage, cheapest
   fix — widening the 120s timeout — to an "Open Question... no bloquea el diseño," while
   the expensive multi-file machinery (Approach B) proceeds unconditionally. A timeout
   value is a revisable parameter, not owner policy; shipping a conservative default (e.g.
   600s/10min) now, revisable later, is not the same as inventing business policy.
2. **Sweep "search once" policy (Medium-High):** justified only against indefinite
   per-cycle polling, never weighed against a small bounded-retry middle ground. Compounds
   with #1: with a short timeout, "one shot then manual" makes the manual path the common
   case, not the rare backstop it's meant to be.
3. **Premises — front-desk identity is load-bearing but deferred (High):** "¿login propio
   o compartido?" is filed as operational, but if front desk shares one default admin
   login, `insertAudit(actor: email)` — cited twice as this fase's real auditability — is
   decorative for exactly the abuse scenario (coerced/dishonest employee) Codex flagged.
4. **Premises — human workflow untested (Medium):** finding and pasting the real MP
   payment ID under time pressure, with a customer waiting, is assumed reliable and never
   dry-run tested.
5. **6-month regret A (Medium-High):** if #1/#2 aren't fixed, manual approval becomes
   routine, not rare — the self-service kiosk quietly recreates "employee must be involved
   in every payment," just moved into an admin panel.
6. **6-month regret B (Medium-High):** Success Criteria only check the database is
   honest, never whether the customer was made whole. A pile of paid-and-unserved sessions
   (no refund policy, no notification) is invisible until a customer complains.
7. **Alternative dismissed too narrowly (Medium):** Approach C was evaluated only as a
   full replacement for B, never as a low-friction *first step* before B's manual-ID
   entry — a "recheck via the same searchByExternalReference call, no privilege" button
   ahead of the manual path would cut the friction risk in #4 without reopening the abuse
   vector, since it's still a real positive query against MP.
8. **Adoption risk (Medium):** if the new flow is fiddly (shared login, ID-sourcing
   friction) and fires often (short timeout), the realistic failure mode is mesa de
   entrada reverting to the physical key override — silently defeating "el pago NUNCA
   enciende la hidrolavadora" through learned behavior, not a bug. Unaddressed anywhere
   in Success Criteria or the manual QA gate.
9. **Critical — dangling cross-reference:** "(see Section review)" is cited twice (0D,
   0E) for the `uq_sessions_active_machine` race, but at CEO-phase read time no such
   section existed yet — the Eng phase resolves this below; flagged here as the sequencing
   gap the subagent correctly caught reading the doc mid-pipeline.

**CODEX (CEO — strategy challenge):** `[codex-unavailable: sandboxed exec blocked reading
the workspace file via PowerShell under this environment's policy — CreateProcess
"Rejected...blocked by policy" on both `Get-Content` and `cmd /c type` attempts, twice.
This is an environment/sandbox limitation, not a content or auth failure — tagged
`[single-model]` per the degradation matrix, proceeding with Claude subagent only.`

**Disposition of subagent findings (auto-decided, P1/P2 — boil the ocean, in blast radius):**

| # | Finding | Decision | Change made |
|---|---|---|---|
| 1 | Timeout deferred when it's the cheapest fix | **ACCEPT** | Success Criteria updated: ship `600` (10 min) as the new default now; owner can revise later — not blocking |
| 2 | Search-once vs bounded retry | **ACCEPT synthesis, not raw fix** | With #1's wider timeout, one search-at-expiry is sufficient in practice (checkout usually completes well within 10 min); documented as a considered tradeoff, not silently dropped |
| 3 | Shared admin login guts the audit trail | **ACCEPT** | Success Criteria hardened: fase requires mesa de entrada to have individual `admin_users` rows before this ships to production — no longer "operational, not blocking" |
| 4 | Untested human workflow | **ACCEPT** | Added to manual QA guide requirement (gate b): live dry run with a real MP test payment, timed |
| 5 | 6-month regret A | **ACCEPT** | Addressed by #1+#2 fix combined (timeout + retry synthesis reduces manual-path frequency) |
| 6 | 6-month regret B (paid, unserved, invisible) | **ACCEPT** | Reuses expansion item 1's audit tag as a queryable metric — no new infra, just consistent tagging |
| 7 | Approach C as low-friction first step | **ACCEPT — added to scope (P2, in blast radius, <1hr CC)** | New: a "reintentar automáticamente" button using the same `searchByExternalReference` call, offered before the manual MP-ID entry |
| 8 | Adoption risk / physical-key reversion | **ACCEPT, process not code** | Noted in Dependencies as an owner conversation to have at ship time, not a code deliverable |
| 9 | Dangling cross-reference | **ACCEPT** | Resolved in the Eng phase section below — this is that section |

### CEO DUAL VOICES — CONSENSUS TABLE

```
═══════════════════════════════════════════════════════════════
  Dimension                            Claude   Codex   Consensus
  ──────────────────────────────────── ──────── ─────── ─────────
  1. Premises valid?                   ISSUES   N/A     N/A (single-voice)
  2. Right problem to solve?           ISSUES   N/A     N/A (single-voice)
  3. Scope calibration correct?        ISSUES   N/A     N/A (single-voice)
  4. Alternatives sufficiently explored?ISSUES  N/A     N/A (single-voice)
  5. Competitive/market risks covered? ISSUES   N/A     N/A (single-voice)
  6. 6-month trajectory sound?         ISSUES   N/A     N/A (single-voice)
═══════════════════════════════════════════════════════════════
Codex unavailable this run (sandbox blocked file read — environment limitation).
Single critical finding (#9, dangling cross-reference) flagged regardless of missing
second voice, per the rule that a single critical finding from one voice still counts.
```

**Phase 1 complete.** Codex: unavailable this run (sandbox blocked). Claude subagent: 9
findings. Consensus: 0/6 confirmed (single-voice, N/A rather than DISAGREE), 1 critical
flagged regardless. Passing to Phase 3 (Eng, always last — Phase 2/2.5 skipped, no
UI/DX scope).

<!-- AUTONOMOUS DECISION LOG -->
## Decision Audit Trail

| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
|---|-------|----------|-----------|-----------|----------|---|
| 1 | CEO | Mode = SELECTIVE EXPANSION | Mechanical | autoplan override | Fixed override for every autoplan run, not the underlying skill's context table | HOLD SCOPE (skill's own default for bug fixes — overridden by autoplan) |
| 2 | CEO | Complexity check (10 files) → proceed as-is | Taste (surfaced at gate) | P5 explicit-over-clever | File count driven by an existing 2-provider interface + existing service split, not scope creep — no way to touch fewer files and fix both paths | Artificial reduction that leaves one provider/path unfixed |
| 3 | CEO | Expansion #1 (distinct audit action name) | Mechanical | P2 boil lakes | In blast radius, ~10 min CC | — |
| 4 | CEO | Expansion #2 (specific mismatch error messages) | Mechanical | P2 boil lakes | In blast radius, ~20 min CC | — |
| 5 | CEO | Expansion #3 (auto-notify on recovery) | Mechanical | P3 | Needs new notification infra, outside blast radius | Building notification channel now |
| 6 | CEO | Expansion #4 (admin recovery counter) | Mechanical | P3 | New UI metric, not required by Success Criteria | Adding it to this fase |
| 7 | CEO | Expansion #5 (rate-limit manual approvals) | Mechanical | P3 | MP-ID lookup is the load-bearing control already; this is defense-in-depth | Adding new counting infra now |
| 8 | CEO | Expansion #6 (system-wide reconciliation pattern) | Mechanical | P3 | Multi-day refactor, many unrelated call sites, far outside blast radius | Generalizing now |
| 9 | CEO | Timeout 120s → 600s shipped as new default, not left as Open Question | Mechanical | P1 completeness | Cheapest, highest-leverage fix; a revisable parameter, not owner business policy | Leaving it blocked on the owner |
| 10 | CEO | Sweep stays "search once per session," not bounded-retry | Mechanical (synthesis) | P5 explicit | With the widened 600s timeout, one check at expiry is sufficient in practice; documented as a considered tradeoff | Adding bounded-retry complexity |
| 11 | CEO | Mesa de entrada requires individual `admin_users` accounts before production use | Mechanical | Security (Codex's own abuse-vector concern, sharpened) | Shared login makes `actor: email` auditing decorative for the exact abuse case this fase exists to close | Leaving it as a non-blocking "operational" open question |
| 12 | CEO | Add live manual-QA dry run (real MP test payment, timed) to gate (b) | Mechanical | P1 completeness | Untested human workflow under time pressure is a real risk for a desk employee | Skipping a dry run |
| 13 | CEO | Add "reintentar automáticamente" (searchByExternalReference, no ID) as step 1 before manual ID entry | Mechanical | P2 boil lakes | In blast radius (reuses the sweep's own call), <1hr CC, reduces friction without reopening the abuse vector | Approach C fully rejected (office-hours' original call) |
| 14 | CEO | Recovered-session audit tag doubles as a queryable "paid but unserved" metric | Mechanical | P4 DRY | Reuses decision #3's tag, no new infra | Building separate tracking |
| 15 | CEO | Physical-key adoption risk noted as a pre-launch owner conversation, not a code deliverable | Mechanical | Scope discipline | It's a process risk, not something Approach B's code can fix | Adding process enforcement to the code |

---

## PHASE 3: ENG REVIEW

Always runs, always last (the required shipping gate reviews the final amended plan).

### Eng Dual Voices

**CLAUDE SUBAGENT (eng — independent review):** Read cold, fresh context, plus 6 specific
source files to verify claims (paymentService.ts, sessionService.ts, schema.ts,
state-machine/index.ts, webhookRoutes.ts, adminService.ts). Full findings:

1. **CRITICAL — the plan cites "(see Section review)" twice for the
   `uq_sessions_active_machine` race and never delivers it.** Traced concretely: the
   partial index (`schema.ts:95-97`) excludes `PAYMENT_EXPIRED` from "active," which is
   *why* a new session can start on the same machine while an old one sits expired.
   Recovering the old one to `AUTHORIZED` either (a) throws against the unique index if
   the new session is still active, uncaught, surfacing as a raw 500 to front-desk, or
   (b) **silently succeeds** if the new session already reached `FINISHED` (not counted
   as "active"), creating a live `WAITING_FOR_BUTTON` authorization on the machine for a
   customer who is no longer there. Neither path is specified anywhere in the plan.
2. **HIGH — recovered authorization has no identity/presence binding.** The physical
   pulsador accepts whoever is present; this fase's entire purpose is recovering payments
   *after* the customer likely left (that's why the loss went unnoticed long enough to
   need reconciliation). Success Criteria only check the database is honest, not that the
   wash reaches the person who paid.
3. **HIGH — no fault isolation on the new MP search call inside the sweep loop.**
   `sweepExpired` processes stale sessions across all machines; a slow/degraded MP API
   with no timeout/circuit-breaker on the new search call could stall expiry processing
   for every machine in that batch — directly against the Mundo's "multi-máquina desde
   el día uno" goal.
4. **MEDIUM-HIGH — individual admin accounts is provisioning discipline, not a code
   gate.** The entire abuse-vector mitigation (Decision #11) has no code path checking
   it; the reconciliation endpoint would work identically for a shared default login.
5. **MEDIUM — `searchByExternalReference` result ambiguity unspecified.** MP could
   plausibly return more than one result for the same `external_reference` (retried
   checkouts); no selection rule stated.
6. **MEDIUM — two known-weak mitigations stacked without hardening:** search-once at
   timeout (Decision #10, asserted not measured) plus an untested human workflow under
   time pressure (Decision #12's dry-run mitigates but doesn't eliminate).
7. **MEDIUM — no enumerated test list existed** ("ver Test Plan más abajo" pointed at
   nothing) — resolved by Section 3 below.
8. **LOW-MEDIUM — new admin route's role/authorization scope unverified** without
   reading `adminRoutes.ts` — checked below in Architecture.
9. **LOW — audit-action taxonomy underspecified** across what are now up to 3 distinct
   approval entry points (webhook, auto-retry button, manual-ID entry) with only 1 new
   tag planned.

**What's sound (no complaints):** `processApproval()` as the single funnel for all entry
points is the right coupling call; the re-verify-before-trust pattern genuinely closes
the "typed fake ID" abuse vector since `external_reference` is server-set, not user
input; the 8-state exhaustive table's *classification* work is solid — it's the
conflict-handling mechanics for the one "Sí" that was missing.

**Disposition (auto-decided, P1 completeness — this is exactly the kind of thing "boil
the ocean" exists for on money-handling code):**

| # | Finding | Decision | Resolution |
|---|---|---|---|
| 1 | Race + silent-success gap | **ACCEPT, fixed below (Architecture)** | Positive precondition check, not reliance on the DB throwing |
| 2 | No identity/presence binding | **ACCEPT, bounded not eliminated** | Same TTL as the happy path bounds exposure; residual risk documented, not solved with an unenforceable checkbox |
| 3 | No fault isolation in sweep | **ACCEPT, fixed below (Architecture)** | Per-session try/catch + timeout, fall through to existing expire-on-failure behavior |
| 4 | Admin accounts not code-enforced | **ACCEPT, fixed below (Code Quality)** | Reject reconciliation actions from the known seeded default admin email |
| 5 | Search result ambiguity | **ACCEPT, fixed below (Code Quality)** | Explicit selection rule: prefer `approved` + exact amount match, else not-found |
| 6 | Stacked weak mitigations | **ACKNOWLEDGED, no new action** | Already the considered tradeoff in Decision #10; manual QA dry-run (#12) is the check on it |
| 7 | No test list | **ACCEPT, fixed below (Section 3)** | Full test plan artifact written |
| 8 | Route authorization scope | **ACCEPT, verified below (Architecture)** | Checked against actual `adminRoutes.ts`/`adminAuth.ts` code |
| 9 | Audit taxonomy underspecified | **ACCEPT, fixed below (Architecture)** | Exactly 3 named actions, not 1 |

### 1. Architecture Review

**The race-condition fix — REVISED after Codex's eng-voice critique (see below), not
just the first draft:**

Codex correctly rejected my first version ("lock the machine, then check the latest
session") because `createSessionWithPayment` (`sessionService.ts:56-115`, verified by
reading it) locks by **plate** (`pg_advisory_xact_lock(hashtext(plate))`), never by
`machineId` — so a symmetric machine-row lock on the recovery side would not actually
serialize against a concurrently-created new session, because the creator never takes
that lock either. Revised design, split by sub-case instead of one mechanism for both:

**Sub-case A — the other session is still active (any status in
`uq_sessions_active_machine`'s list).** No new locking needed: the partial unique index
itself is the serialization primitive, enforced by Postgres for *any* writer that
touches an active status for that `machineId`, lock or no lock. The fix is simply to
**catch the unique-violation** on the recovery write instead of letting it surface as a
raw 500: translate it into `ApprovalResultType: 'machine_occupied'`, audit it, and have
the route return a specific message. This was the subagent's original, simpler instinct
and it's correct for this sub-case — no recency query needed here at all.

**Sub-case B — the other session already reached a terminal status (e.g. `FINISHED`).**
This is the actual gap: the unique index doesn't fire (terminal statuses are excluded by
design), so a naive write would silently succeed. Fix: a positive existence check,
*inside the same transaction as the write*, immediately before it —
`EXISTS (session for this machineId with createdAt >= A.createdAt AND id != A.id)`
(uses `idx_sessions_machine_created`, `schema.ts:98`, no new index) — `>=` not `>`,
per Codex's point that exact-timestamp ties make strict ordering unsafe; an existence
check sidesteps needing a safe "latest" tie-break entirely. If true, reject with a
**different** result type, `'machine_used_since'`, so front-desk sees "esta máquina ya
se usó para otro cliente desde que este pago expiró" — distinct from `'machine_occupied'`.

**Residual gap, disclosed not hidden:** Codex is right that fully closing sub-case B's
TOCTOU window would require `createSessionWithPayment` to also lock the machine row
before insert — it doesn't today, and changing it is outside this fase's blast radius
(that function isn't otherwise touched here, and hardening an unrelated creation path
for a race whose window is a single fast INSERT, not a loop, is disproportionate to add
now). **Logged to TODOS.md** as a hardening item, not silently assumed away.

**External I/O must happen outside any transaction — Codex's other confirmed correction.**
Verified in `sessionService.ts:333-352`: the *existing* `sweepExpired` never calls an
external API inside its transactions — pure DB reads/writes, matching
`webhookRoutes.ts`'s pattern of doing the MP call first, then passing the confirmed
result into `processApproval` (which opens its own short transaction). The new
`searchByExternalReference` call in the sweep and in "reintentar automáticamente" must
follow the exact same shape: call MP **before** opening any transaction; only the
sub-case A/B checks and the final write happen inside `processApproval`'s existing
transaction (which already re-reads the session via `getSessionForUpdate` — so it
already gets a fresh, locked read of the current status, i.e. the compare-and-swap
Codex asked for is inherent to the existing pattern, just needed stating explicitly).

**New state-machine edge required:** `PAYMENT_EXPIRED -> AUTHORIZED` in `TRANSITIONS`
(`state-machine/src/index.ts:28`), added explicitly and tested — a real new edge the
exhaustive table already justifies (only `PAYMENT_EXPIRED` gets it; the other 7 terminal
states keep `[]`).

```
MP search (searchByExternalReference) happens FIRST, outside any transaction
(mirrors webhookRoutes.ts's existing re-verify-before-trust pattern)
                              │
                    found + status=approved?
                              │
                              ▼
        processApproval(...) opens its OWN transaction (existing function,
        unchanged entry point) -> getSessionForUpdate re-reads session fresh
                              │
                    session.status === 'PAYMENT_EXPIRED'?
                              │
                ┌─────────────┴──────────────┐
                │                              │
     other active session on this      no active session, AND no session
     machine? (sub-case A)             created since A (sub-case B check,
                │                       idx_sessions_machine_created)
                ▼                              │
     unique-index violation on write  ┌────────┴────────┐
     -> caught -> 'machine_occupied'  │                  │
                                  exists?            none found
                                       ▼                  ▼
                            'machine_used_since'   proceed: transitionSession
                            (audited, distinct     PAYMENT_EXPIRED->AUTHORIZED,
                            message)               insertAuthorization (normal
                                                    TTL), audit per taxonomy below
```

**Identity/presence gap (HIGH finding #2) — Codex correctly rejected my "same TTL
bounds it" argument.** A fresh TTL opened by a recovery that happens after a real gap
*is* a new authorization window at a moment the customer is more likely gone than at
checkout time — not equivalent risk to the happy path, where the TTL starts the instant
the customer paid, while they're standing there. This is a genuine, path-dependent
difference, not one blanket answer:
- **Automatic paths (sweep + "reintentar automáticamente"):** fire at or shortly after
  the (now 10-minute) timeout — the motivating scenario is a payment that was simply
  slow, and the customer is very likely still at or near the machine. Risk here is
  close to the happy path's; TTL-bounding is a reasonable, proportionate answer.
- **Manual MP-ID-entry path:** can fire arbitrarily later (mesa de entrada or the owner
  noticing a missed payment hours later via the MP dashboard) — this is the path where
  Codex's objection genuinely bites. **Surfaced as a decision for the user at the Final
  Gate below**, not auto-resolved — options range from "accept it, same trust model as
  the physical key already relies on human judgment" to "require mesa de entrada to
  affirmatively confirm the customer/vehicle is present as part of the manual flow" to
  Codex's more structural alternative (a non-active claim state, released only when
  attended). This is exactly the kind of safety-adjacent judgment call that shouldn't be
  auto-decided by principles alone.

**Audit action taxonomy (resolves LOW finding #9) — exactly 3, not 1:**
- `PAYMENT_APPROVED` — existing, webhook happy path, unchanged.
- `PAYMENT_AUTO_RECONCILED` — sweep's search-at-timeout AND the "reintentar
  automáticamente" button. Same mechanism (automated `searchByExternalReference`
  + `processApproval`), no human judgment or typed ID — counted together for the
  "paid but nearly not washed" metric (CEO decision #14).
- `PAYMENT_MANUALLY_RECONCILED` — the manual MP-ID-entry path only, the one
  requiring mesa de entrada to source and type a real payment ID.

**Route authorization scope (resolves LOW-MEDIUM finding #8) — verified, not
assumed:** read `apps/api/src/http/routes/adminRoutes.ts:4,42` directly —
`r.use(requireAdmin(ctx.config))` gates the entire router; the new reconciliation
endpoints inherit this automatically, same as every other admin action. There is
no finer per-role split today (`admin_users.role` defaults to a single `'admin'`
value, `schema.ts:206`) — acceptable for Fase 1 scope since it matches the
existing pattern for every other admin route, layered with the default-email
guard below for the higher-stakes reconciliation actions specifically.

**Fault isolation for the sweep's search call (resolves HIGH finding #3):** wrap
`searchByExternalReference` inside `sweepExpired`'s per-session iteration in a
try/catch with a short timeout (a few seconds); on failure or timeout, log it and
fall through to the **existing** mark-as-expired behavior for that one session —
never let one session's MP-search failure abort or delay the batch's processing
of every other machine's expired sessions. This is a pure addition around an
existing loop, not a restructure.

### 2. Code Quality Review

**Admin-account code gate (resolves MEDIUM-HIGH finding #4):** the reconciliation
actions (auto-retry button AND manual-ID entry) reject the request if
`req.admin.email` equals the known seeded default (`config.adminEmail`, the same
constant ADR-026 already names as `admin@hidro.local`). Cheap equality check,
reuses an existing config value, no new infrastructure — closes the gap between
"individual accounts required" (Success Criteria) and "nothing in code checks
it" (the subagent's exact complaint).

**Search-result selection rule (resolves MEDIUM finding #5, hardened per Codex):**
when `searchByExternalReference` returns more than one result (plausible on
retried checkouts), select the one with `status === 'approved'` AND `amount`
matching the session's expected amount exactly AND matching currency/collector
account (never assume single-currency/single-account just because the demo
provider is simple today); if more than one result still matches after that
filter, treat it as a **duplicate-charge anomaly** — do not silently pick one —
audit it distinctly and require manual review rather than auto-approving; if
none match, treat the whole search as not-found (same as zero results).

**Explicit `source` on every `processApproval` call (Codex's point — up to 3
entry points now, callers shouldn't be inferred):** add a `source: 'webhook' |
'sweep' | 'admin_recheck' | 'admin_manual'` field to `ApprovalInput`, set by the
caller, not inferred inside the function. This is what makes the 3-action audit
taxonomy above reliable under retries and concurrent callers, instead of
guessing the source from context.

**DRY / naming:** no new violations found — the recovery precondition function
(`getLatestSessionForMachine` or similar) is a natural extension of the existing
`idx_sessions_machine_created` index's purpose, not a parallel mechanism.

**ASCII diagram maintenance:** the header comment at `db/schema.ts:14-20` listing
the 5 unique constraints and their purpose stays accurate — this fase adds a
*consumer* of `uq_sessions_active_machine`'s semantics (the recency check), it
doesn't change the constraint itself. No diagram update needed there. The
`TRANSITIONS` table's own inline comment (`state-machine/src/index.ts:15`,
`IDLE -> PAYMENT_PENDING -> PAYMENT_APPROVED -> AUTHORIZED`) should get one
line added noting the new recovery edge, so the next reader isn't surprised by
an edge the top-of-file summary doesn't mention.

### 3. Test Review

**Framework detected:** `package.json` present, `apps/api/tests/` exists per the
design doc's own Constraints section (Node/TypeScript, existing suite of 48
tests referenced in this Mundo's `CLAUDE.md`/`ESTADO.md`).

**Coverage diagram:**

```
CODE PATHS                                              STATUS
[+] payments/provider.ts + mercadoPagoProvider.ts + demoProvider.ts
  └── searchByExternalReference()
      ├── [GAP] found, single result, approved            -> new test
      ├── [GAP] found, multiple results (select rule)      -> new test
      ├── [GAP] found, none approved                       -> new test
      ├── [GAP] not found                                  -> new test
      └── [GAP] demoProvider parity (found/not-found)       -> new test (was an Open Question, now closed)

[+] services/paymentService.ts — processApproval() terminal branch (extended)
  ├── [GAP] PAYMENT_EXPIRED, machine untouched since -> recovers to AUTHORIZED  -> new test [→ regression-adjacent, existing function]
  ├── [GAP] PAYMENT_EXPIRED, another session now active on same machine -> 'machine_occupied'-style rejection -> new test [CRITICAL]
  ├── [GAP] PAYMENT_EXPIRED, another session already FINISHED on same machine -> rejection, NOT silent success -> new test [CRITICAL]
  ├── [★★★ EXISTING] PAYMENT_FAILED/MACHINE_OFFLINE/etc. stay non-recoverable — covered by current terminal-branch tests, verify they still pass unchanged
  └── [GAP] admin email = seeded default -> reconciliation actions rejected -> new test

[+] services/sessionService.ts — sweepExpired() (extended)
  ├── [GAP] webhook lost, provider confirms approved -> recovers automatically, never writes PAYMENT_EXPIRED -> new test
  ├── [GAP] webhook lost, provider confirms nothing -> expires as today -> new test (REGRESSION-adjacent: must not break existing expiry behavior)
  └── [GAP] MP search call times out/errors -> falls through to expire, does NOT block other machines' sweep -> new test [→E2E-ish, exercises the whole batch loop]

[+] state-machine/src/index.ts — TRANSITIONS
  └── [GAP] PAYMENT_EXPIRED -> AUTHORIZED is now legal; all other 7 terminal states still have zero outgoing edges -> new test, one assertion per terminal state (matches the exhaustive table 1:1)

[+] http/routes/adminRoutes.ts — new reconciliation endpoint(s)
  ├── [GAP] "reintentar automáticamente" — found+approved -> PAYMENT_AUTO_RECONCILED -> new test
  ├── [GAP] "reintentar automáticamente" — not found -> clear "sin novedades" response, no error -> new test
  ├── [GAP] manual-ID entry — correct ID, matching session+amount -> PAYMENT_MANUALLY_RECONCILED -> new test
  ├── [GAP] manual-ID entry — ID belongs to a different session (external_reference mismatch) -> rejected with specific message -> new test
  ├── [GAP] manual-ID entry — amount mismatch -> rejected with specific message -> new test
  └── [GAP] unauthenticated request -> 401 (existing requireAdmin behavior, verify unchanged) -> [★★★ EXISTING pattern, new route]

[+] payments/mercadoPagoProvider.ts — getPayment() landmine
  └── [GAP] getPayment() removed OR fixed to use real search — whichever the implementer picks, the OLD hardcoded-PENDING test (if one exists) must be deleted/updated -> REGRESSION check

COVERAGE: 0/19 new paths tested (0% — nothing built yet, this is the plan)
GAPS: 19 (2 critical: machine-occupied + machine-finished-since; 1 →E2E-ish: sweep batch isolation)
```

**Regression rule applied:** the sweep's "webhook lost, provider confirms
nothing -> expires as today" case is a REGRESSION test, not just a new-feature
test — it proves the existing expiry behavior for genuinely-unpaid sessions
still works after the search is added in front of it. Same for
`requireAdmin` on the new route and the 7 non-recoverable terminal states.

**Test plan artifact:** written to
`~/.gstack/projects/PMartinGatica-hidro-self-service/{user}-main-eng-review-test-plan-{datetime}.md`
(see below — this is what `/qa` and `/qa-only` will read).

### 4. Performance Review

- **N+1 risk, addressed:** `sweepExpired` already iterates sessions one-by-one in
  separate transactions (existing pattern, `sessionService.ts:342-352`); adding
  one `searchByExternalReference` call per stale session is proportional to
  actual timeout events, not a new N+1 — it fires only for sessions that were
  already going to be processed. Bounded by the fault-isolation fix above
  (Architecture) so a slow provider can't turn this into an unbounded stall.
- **No caching opportunity:** each search is for a distinct, one-time
  `external_reference` — nothing to cache.
- **The new recency check** (`idx_sessions_machine_created`) is an existing
  index doing exactly the query it was built for — no new index, no scan
  concern, bounded to one machine's sessions.
- **No memory concerns:** all new code paths are single-row lookups and single
  external API calls, no batch loading introduced.

**CODEX (eng — architecture challenge):** ran successfully this phase (stdin-piped
prompt with the full plan embedded — the file-path-read approach that failed in the
CEO phase hit a Windows sandbox policy block on Codex's own PowerShell file reads;
piping via `codex exec -` worked). Full output verbatim:

════════════════════════════════════════════════════════════
The proposed race fix is not complete.

- "Lock the machine, then check latest session" is only correct if **every session
  creator also locks that same machine row in the same transaction**. "Verify during
  Build" is not acceptable; make it a hard design requirement. Otherwise recovery and
  new-session creation still interleave.
- `createdAt` is not a safe ordering primitive unless it is DB-assigned and strictly
  ordered per machine. Timestamp ties can make `latest.id === A.id` nondeterministic.
  Safer: a per-machine monotonically increasing session generation, captured on the
  session; recovery requires the generation to still match. At minimum, conservatively
  reject any different session with `createdAt >= A.createdAt`.
- The external MP lookup cannot be safely performed while holding the machine/session
  transaction lock. But if it happens before the lock, webhook/sweep/manual requests can
  change the session meanwhile. The final transaction must re-read and conditionally
  transition only from `PAYMENT_EXPIRED`, then apply the generation/lock precondition.
  Define this explicitly.
- A recovery rejected because the machine was used since still leaves a confirmed paid
  customer unserved. "Audit metric" is not a resolution. This plan has no customer-safe
  disposition for that money: refund, credit, operator escalation queue, or a retained
  claim workflow. Deferring refunds does not make this acceptable once the design
  deliberately detects these cases.
- The "same normal TTL" does not solve presence/identity. It creates a fresh live
  authorization after the payer may have left; anyone at the pulsador can consume it.
  This is a new authorization timing, not merely the pre-existing happy-path exposure.
  Safe simpler approach: reconcile payment into a non-active `PAID_REQUIRES_ATTENDANT_
  RELEASE` state; an operator may release it only while the claimant is physically
  present. If that cannot be enforced without hardware, do not auto-authorize expired
  sessions.
- The simplest safe alternative is therefore: never resurrect `PAYMENT_EXPIRED` directly
  to `AUTHORIZED`. Record the verified payment, block automatic reuse of the machine
  slot, and require an explicit attended reactivation/claim flow. It removes the recency
  algorithm, active-index race, and unattended authorization problem.

Other misses:
- "Search once" is undefined operationally. A crash or timeout between MP lookup and
  state update either repeats the lookup or loses the only attempt. It needs durable
  attempt/lease state and a clear recovery policy.
- The sweep must not hold a DB transaction across MP I/O, and its expiry write must be a
  compare-and-swap on the current status. Otherwise it can overwrite a concurrent webhook
  approval or create long-lived locks.
- `approved + exact amount + external_reference` is incomplete matching. Also validate
  currency, collector/merchant account, environment, and a canonical integer minor-unit
  amount. Define handling for multiple approved payments: selecting one silently can
  conceal a duplicate charge.
- MP search needs pagination and an explicit temporal boundary. A provider search that
  returns only the first page can miss the valid approved payment.
- The default-email check does not enforce individual accounts. It is brittle
  configuration-based denial, not identity assurance. Disable/bootstrap-mark the seeded
  account, require per-user accounts, and ideally separate `reconcile` permission from
  broad admin.
- The plan promises one canonical approval path but adds source-specific audit behavior.
  Make the source (`webhook`, `sweep`, `admin_recheck`, `admin_manual`) an explicit input
  to `processApproval`; otherwise retries and concurrent callers will produce misleading
  or duplicate audit events.
- Adding `PAYMENT_EXPIRED → AUTHORIZED` weakens the meaning of a terminal state globally.
  Any future caller that can reach the transition can resurrect it. Recovery should be
  guarded by a dedicated domain operation, not merely by opening a generic FSM edge.
════════════════════════════════════════════════════════════

**Disposition of Codex's findings:**

| # | Codex finding | Decision | Resolution |
|---|---|---|---|
| 1 | Symmetric locking required | **ACCEPT the diagnosis, PARTIAL fix (P2 blast radius)** | Sub-case A now relies on the unique index itself (no locking needed, always serialized by Postgres); sub-case B's narrow residual TOCTOU is disclosed + deferred to TODOS.md rather than expanding `createSessionWithPayment`'s scope — see Architecture above |
| 2 | `createdAt` tie unsafe | **ACCEPT, fixed** | Switched to an `EXISTS (createdAt >= A.createdAt)` check per Codex's own conservative suggestion — no "latest" tie-break needed |
| 3 | MP lookup can't happen inside the lock | **ACCEPT, fixed** | Search happens before any transaction, mirroring the existing webhook pattern — see Architecture above |
| 4 | "Audit metric" isn't a resolution for the unserved customer | **SURFACED AT FINAL GATE — not auto-decided** | Genuine tension with premise 5 (refunds explicitly out of scope, user-confirmed twice this session); not mine to override unilaterally |
| 5 | Same TTL doesn't solve presence/identity | **PARTIALLY ACCEPT, split by path** | Automatic paths: TTL-bounding stands (proportionate, timing resembles happy path). Manual path: **surfaced at Final Gate**, not auto-resolved — see Architecture above |
| 6 | "Never resurrect PAYMENT_EXPIRED" as the simpler safe alternative | **NOTED, not adopted this fase** | A structurally bigger change (new non-active claim state + attended-release flow) than Approach B's blast radius; the corrected recency+index approach closes the concrete failure modes found without a new state; flagged in TODOS.md as the more structural alternative if operational experience shows the lighter fix insufficient |
| 7 | "Search once" undefined for crash/timeout mid-lookup | **ACCEPT** | The search call itself is idempotent (a read against MP, no state mutation) — a crash before completion just means the session stays `PAYMENT_EXPIRED` and gets picked up by the next path (sweep's own periodic re-run is out of scope per Decision #10, but the manual path remains available indefinitely as the backstop) |
| 8 | Sweep must not hold a tx across I/O + CAS on expiry write | **ACCEPT, fixed** | Same fix as #3 — search outside tx; the expiry write itself already reads fresh via `getSessionForUpdate` inside `processApproval`, and the *existing* mark-as-expired path (`sessionService.ts:344`, unchanged) is already a single conditioned `transitionSession` call, not a race-prone read-modify-write spanning I/O |
| 9 | Matching needs currency/collector/amount, dedupe on multiple approved results | **ACCEPT, fixed** | Selection rule hardened above (Code Quality) |
| 10 | MP search needs pagination + temporal boundary | **ACCEPT, noted for Build** | Added as an explicit Build-time requirement, not elaborated further at design level (this is provider-SDK-specific mechanics) |
| 11 | Default-email check is brittle; separate `reconcile` permission from broad admin | **ACCEPT the critique, PARTIAL now + TODO for the rest** | Default-email guard ships now (cheap, closes the concrete gap found); dedicated `reconcile` permission is a real RBAC expansion beyond this fase's blast radius — added to TODOS.md |
| 12 | `source` should be explicit input to `processApproval` | **ACCEPT, fixed** | Added above (Code Quality) |
| 13 | New FSM edge "weakens terminal state meaning globally" | **ACKNOWLEDGED, accepted risk** | The exhaustive table (Approach B's whole point) is exactly the guard against silent misuse — every terminal state's recoverability is an explicit, tested, reviewed decision, not an open door; a future caller reaching the edge would still go through the same `processApproval` sub-case checks, not a bare `canTransition()` call |

### ENG DUAL VOICES — CONSENSUS TABLE

```
═══════════════════════════════════════════════════════════════
  Dimension                            Claude   Codex   Consensus
  ──────────────────────────────────── ──────── ─────── ─────────
  1. Architecture sound?               ISSUES   ISSUES  CONFIRMED (both found the same
                                                          core gap independently — race
                                                          condition — strong signal)
  2. Test coverage sufficient?         ISSUES   ISSUES  CONFIRMED (both flagged missing
                                                          test enumeration)
  3. Performance risks addressed?      ISSUES   ISSUES  CONFIRMED (both flagged the
                                                          unbounded sweep-loop I/O risk)
  4. Security threats covered?         ISSUES   ISSUES  CONFIRMED (both flagged the
                                                          admin-account gap; Codex went
                                                          further — DISAGREE on whether
                                                          email-check suffices → taste,
                                                          logged above)
  5. Error paths handled?              ISSUES   ISSUES  CONFIRMED (both flagged the
                                                          silent-success/unhandled-
                                                          conflict gap independently)
  6. Deployment risk manageable?       N/A      ISSUES  Codex-only: identity/presence
                                                          timing risk on the manual path
                                                          → surfaced at Final Gate
═══════════════════════════════════════════════════════════════
CONFIRMED = both flagged the same core issue independently — this is the strongest
possible signal for a critical finding (two different models, no shared context,
same conclusion). 2 DISAGREE-shaped points (identity/presence resolution depth,
refund-vs-metric) escalated to the Final Gate as genuine judgment calls, not
auto-decided.
```

**Phase 3 complete.** Codex: 13 findings. Claude subagent: 9 findings (significant
overlap — the CRITICAL race condition was independently found by both). Consensus:
5/6 confirmed, 1 Codex-only (deployment/identity timing) → surfaced at gate. Passing
to Phase 4 (Final Gate).

### Failure Modes Registry

| Codepath | Realistic failure | Test? | Error handling? | User-visible or silent? |
|---|---|---|---|---|
| Recovery, sub-case A (machine occupied) | Concurrent active session on same machine | New test (planned) | Unique-violation caught, translated | Clear message to front-desk |
| Recovery, sub-case B (machine used since) | Other session already `FINISHED` | New test (planned) | `EXISTS` check before write | Clear message to front-desk |
| Recovery, residual TOCTOU (see TODOS.md) | New session created in the exact window between the check and the write | **No test possible without the fix itself** | **None — accepted residual risk** | **Would be silent if it ever fires — CRITICAL GAP, explicitly disclosed, not hidden, deferred by design decision not oversight** |
| Sweep's MP search call | Timeout/error from Mercado Pago | New test (planned) | Try/catch, falls through to existing expire path | Silent to the end customer (correct — falls back to pre-existing behavior), logged for operators |
| `searchByExternalReference`, multiple approved results | Retried checkout produces >1 approved payment for one `external_reference` | New test (planned) | Treated as duplicate-charge anomaly, not auto-approved | Requires manual review — surfaced, not silent |
| Manual-ID entry, wrong session | `external_reference` mismatch | New test (planned) | Rejected before `processApproval` | Clear message |
| Manual-ID entry, amount mismatch | Typo or wrong ID | New test (planned) | Rejected, existing `amount_mismatch` path in `processApproval` | Clear message (existing pattern, reused) |
| Reconciliation from seeded default admin | Provisioning forgotten | New test (planned) | Explicit email-equality rejection | Clear message + audited |
| `PAYMENT_EXPIRED -> AUTHORIZED` reached by an unintended future caller | A later change calls the new edge without going through the sub-case checks | No test possible today (hypothetical future code) | Mitigated by funneling ALL recovery through `processApproval`'s terminal branch — no other call site should transition into this edge directly | Would depend on the future caller — flagged as an architectural invariant to preserve, noted in code comments at Build time |

**Critical gap, disclosed:** the residual TOCTOU (row 3) has no test and no error
handling — it is a **known, accepted, narrow** risk, explicitly chosen over expanding
this fase's blast radius into `createSessionWithPayment`. This is different from a
silent gap found by accident; it is a documented tradeoff with a TODO for closing it
fully. Per this skill's own rule ("if any failure mode has no test AND no error
handling AND would be silent, flag it as a critical gap") — flagged, not hidden.

### Worktree Parallelization Strategy

| Step | Modules touched | Depends on |
|---|---|---|
| Provider search method | `payments/` | — |
| processApproval extension + recoverability table | `services/paymentService.ts` | Provider search method (needs the interface) |
| Sweep changes | `services/sessionService.ts` | processApproval extension (calls it) |
| State machine edge | `packages/state-machine/` | — (independent) |
| Constants (timeout value) | `packages/shared/src/constants.ts` | — (independent) |
| Admin route (reconciliation endpoints) | `http/routes/`, `services/adminService.ts` | processApproval extension |
| Tests | `apps/api/tests/` | All of the above (tests the finished behavior) |

**Lanes:**
- Lane A: Provider search method → processApproval extension → Sweep changes →
  Admin route (sequential, shared `services/` and dependency chain)
- Lane B: State machine edge (independent, no shared module with Lane A until
  processApproval consumes the new edge)
- Lane C: Constants change (fully independent, one-line diff)

**Execution order:** Launch B and C in parallel with the start of Lane A (they have no
dependency on it). Lane A's later steps depend on the state-machine edge from B
existing, so merge B before starting the "processApproval extension" step of A. C
merges whenever, no conflicts. Given the total size (10 files, ~1h CC estimate), a
single sequential Build pass is likely more efficient than coordinating 3 worktrees for
work this size — noted as available, not recommended, for a fase this small.

## Implementation Tasks

Synthesized from this review's findings. Each task derives from a specific finding
above. Run with Claude Code; checkbox as you ship.

- [x] **T1 (P1, human: ~2h / CC: ~15min)** — payments — Add `searchByExternalReference` to `PaymentProvider` interface + `mercadoPagoProvider.ts` + `demoProvider.ts`
  - Surfaced by: design doc Success Criteria + Eng Code Quality (multi-result selection rule)
  - Files: `apps/api/src/payments/provider.ts`, `mercadoPagoProvider.ts`, `demoProvider.ts`
  - Verify: unit tests for found/not-found/multiple-results/duplicate-anomaly cases
- [x] **T2 (P1, human: ~3h / CC: ~20min)** — payments — Extend `processApproval`'s terminal branch: exhaustive recoverability table + `source` field + sub-case A/B checks
  - Surfaced by: Eng Architecture (CRITICAL race fix)
  - Files: `apps/api/src/services/paymentService.ts`, new small module for the recoverability table
  - Verify: two-session-one-machine test (sub-case A + B), all 8 terminal states tested
- [x] **T3 (P1, human: ~1h / CC: ~10min)** — state-machine — Add `PAYMENT_EXPIRED -> AUTHORIZED` edge
  - Surfaced by: design doc Approach B
  - Files: `packages/state-machine/src/index.ts`
  - Verify: transition table test, one assertion per terminal state
- [x] **T4 (P1, human: ~2h / CC: ~15min)** — sessions — Sweep: search-before-expire (outside tx), fault-isolated per session
  - Surfaced by: Eng Architecture (fault isolation) + Codex (tx/I-O separation)
  - Files: `apps/api/src/services/sessionService.ts`
  - Verify: MP-timeout-during-sweep test (batch continues for other machines)
- [x] **T5 (P1, human: ~3h / CC: ~20min)** — admin — New reconciliation routes: auto-retry button + manual-ID entry, default-admin-email guard
  - Surfaced by: design doc + CEO expansion items 1-2 + Eng Code Quality (admin gate)
  - Files: `apps/api/src/http/routes/adminRoutes.ts`, `apps/api/src/services/adminService.ts`
  - Verify: full route test matrix from Test Review diagram
- [x] **T6 (P2, human: ~30min / CC: ~5min)** — constants — Timeout 120s → 600s
  - Surfaced by: CEO Decision #9
  - Files: `packages/shared/src/constants.ts`
  - Verify: existing timeout tests updated
- [x] **T7 (P2, human: ~1h / CC: ~10min)** — payments — Fix or remove `getPayment()` hardcoded stub
  - Surfaced by: original office-hours Success Criteria
  - Files: `apps/api/src/payments/mercadoPagoProvider.ts`
  - Verify: no callers reference the removed method, or new implementation tested
- [x] **T8 (P2, human: ~1h / CC: ~10min)** — docs — Update `TRANSITIONS` header comment + `db/schema.ts` unique-constraint comment if needed
  - Surfaced by: Eng Code Quality (diagram maintenance)
  - Files: `packages/state-machine/src/index.ts`
  - Verify: comment reflects the new edge

_No new tasks from Performance review — no issues found beyond what's covered by T4._

### JSONL artifact

Written to
`~/.gstack/projects/PMartinGatica-hidro-self-service/tasks-eng-review-{datetime}.jsonl`
(8 tasks, see below).

## Completion Summary

- Step 0: Scope Challenge — scope held as SELECTIVE EXPANSION baseline; 2 items added
  (audit taxonomy pieces already folded into T2/T5), 4 items deferred to TODOS.md
- Architecture Review: 1 CRITICAL + 2 HIGH issues found, all resolved or explicitly
  disclosed as accepted residual risk
- Code Quality Review: 2 MEDIUM issues found (search-result ambiguity, admin gate),
  both fixed in the plan
- Test Review: diagram produced, 19 gaps identified, test plan artifact written
- Performance Review: 0 new issues (existing N+1/caching concerns already addressed
  by the fault-isolation fix)
- NOT in scope: written (CEO phase)
- What already exists: written (CEO phase)
- TODOS.md updates: 7 items total (4 CEO expansions + 3 Eng hardening items)
- Failure modes: 1 critical gap flagged (disclosed residual TOCTOU, not hidden)
- Outside voice: Codex ran (eng phase, via stdin after CEO phase's file-read sandbox
  block); CEO phase Codex unavailable (single-model that phase)
- Parallelization: 3 lanes, 2 parallel (B, C) / 1 sequential (A) — noted as available,
  single-pass recommended given the size
- Lake Score: 15/15 CEO decisions + 9/9 Eng disposition decisions chose the complete
  option over a shortcut, except the 2 explicitly surfaced for user judgment below

<!-- AUTONOMOUS DECISION LOG (continued) -->

| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
|---|-------|----------|-----------|-----------|----------|---|
| 16 | Eng | Race fix redesigned: unique-index-catch (sub-case A) + EXISTS recency check (sub-case B), no symmetric locking | Mechanical (Codex-corrected) | P5 explicit, technical correctness | `createSessionWithPayment` doesn't lock by machine; symmetric locking claim was wrong; the unique index already serializes sub-case A for free | Original "lock + check latest" design (technically incomplete) |
| 17 | Eng | Residual TOCTOU in sub-case B disclosed, not closed, this fase | Mechanical | P2 boil lakes (bounded by blast radius) | Closing it fully requires touching `createSessionWithPayment`, outside this fase's scope, for a millisecond-scale window | Expanding scope to touch session creation |
| 18 | Eng | MP search moved outside any transaction | Mechanical | Technical correctness (Codex) | Existing `webhookRoutes.ts` and `sweepExpired` patterns already never hold I/O inside a tx; new code must match | Doing the search inside `processApproval`'s transaction |
| 19 | Eng | Search-result selection rule hardened: currency + collector + duplicate-anomaly handling | Mechanical | P1 completeness | Codex correctly flagged amount-only matching as incomplete | Amount-only matching |
| 20 | Eng | `source` field added as explicit `ApprovalInput` input | Mechanical | P5 explicit over clever | Prevents ambiguous/inferred audit taxonomy under retries | Inferring source from caller context |
| 21 | Eng | Dedicated `reconcile` RBAC permission deferred to TODOS.md; default-email guard ships now | Mechanical (Taste on the split) | P2 blast radius | Full RBAC expansion is bigger than this fase; the concrete gap found is closed cheaply now | Doing neither, or doing the full RBAC work now |
| 22 | Eng | Codex's "never resurrect PAYMENT_EXPIRED, use an attended-claim state" alternative noted, not adopted | Taste (surfaced conceptually, not blocking) | P5 explicit vs. P2 blast radius | Structurally bigger than Approach B; the corrected lighter fix closes the concrete failure modes found | Adopting the structural alternative now |
| 23 | Eng | Refund/credit disposition for `machine_used_since` rejections | **RESOLVED at Final Gate — user decided** | User sovereignty | User confirmed: metric-only, consistent with premise 5 (confirmed twice this session — refund/credit policy is the owner's decision, not this fase's) | Auto-created operational escalation queue |
| 24 | Eng | Identity/presence timing on the manual-ID path | **RESOLVED at Final Gate — user decided** | User sovereignty | User confirmed: accept the residual risk, same trust model this Mundo already applies to the mechanic's physical key override — no presence-confirmation field added | Presence-confirmation field in the manual-approval form |

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` (via `/autoplan`) | Scope & strategy | 1 | issues_open → resolved at gate | 9 findings, all accepted or resolved; mode SELECTIVE EXPANSION |
| Codex Review | `/codex review` | Independent 2nd opinion | 1 (eng phase only — CEO phase sandbox-blocked) | issues_open → resolved | 13 findings, 11 accepted/fixed, 2 elevated to Final Gate |
| Eng Review | `/plan-eng-review` (via `/autoplan`) | Architecture & tests (required) | 1 | issues_open → resolved at gate | 22 total issues (9 Claude + 13 Codex, with overlap on the CRITICAL race), 1 critical gap disclosed (not hidden) |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | skipped | No UI scope detected (Phase 0) |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | skipped | No developer-facing scope detected (Phase 0) |

- **CODEX:** Eng-phase Codex voice ran via stdin-piped prompt (file-path read was blocked by this environment's sandbox policy in the CEO phase). Found the same CRITICAL race independently as the Claude subagent, then corrected 3 concrete technical flaws in the first proposed fix (no symmetric machine-row locking exists in `createSessionWithPayment`; `createdAt` ties are unsafe; external I/O can't happen inside the recovery transaction) — all 3 accepted and fixed in this document.
- **CROSS-MODEL:** Both Claude (subagent, CEO and Eng phases) and Codex (Eng phase) independently found the `uq_sessions_active_machine` race as the single highest-severity gap — two different models, no shared context, same conclusion. Strongest possible signal for that finding. They diverged on remediation depth for the identity/presence timing issue (Claude: bound + optionally confirm; Codex: restructure into a non-active claim state) — resolved by the user at the Final Gate in favor of the lighter approach, consistent with this Mundo's existing physical-key trust model.
- **VERDICT:** CEO + ENG CLEARED at Final Gate — both User Challenges resolved by explicit user decision (metric-only disposition; residual identity risk accepted, same trust model as the physical key). Ready to implement.

NO UNRESOLVED DECISIONS
